name: Test

on:
  push:
    branches:
      - feature

jobs:
  Test:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Prepare Java
        uses: agilecustoms/gha-prepare-java@feature
        with:
          AWS_ACCOUNT: ${{ vars.AWS_ACCOUNT_SHARED }}
